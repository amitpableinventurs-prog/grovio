import { Info } from 'lucide-react';
import ContentPageView from '../components/ContentPageView';

export default function AboutUsPage() {
  return <ContentPageView slug="about-us" icon={<Info size={20} />} />;
}
