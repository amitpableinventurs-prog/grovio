import { ShieldCheck } from 'lucide-react';
import ContentPageView from '../components/ContentPageView';

export default function PrivacyPolicyPage() {
  return <ContentPageView slug="privacy-policy" icon={<ShieldCheck size={20} />} />;
}
