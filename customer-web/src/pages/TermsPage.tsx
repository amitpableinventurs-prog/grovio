import { FileText } from 'lucide-react';
import ContentPageView from '../components/ContentPageView';

export default function TermsPage() {
  return <ContentPageView slug="terms-and-conditions" icon={<FileText size={20} />} />;
}
